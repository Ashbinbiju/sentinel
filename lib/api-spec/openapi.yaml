openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: API specification
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: stocks
    description: Stock market data
paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"
  /stocks/market-indices:
    get:
      operationId: getMarketIndices
      tags: [stocks]
      summary: Get broad market indices
      responses:
        "200":
          description: Market indices data
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/MarketIndex"
        "500":
          description: Failed to fetch data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /stocks/sectors:
    get:
      operationId: getSectors
      tags: [stocks]
      summary: Get sector performance
      responses:
        "200":
          description: Sector performance data
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SectorPerformance"
        "500":
          description: Failed to fetch data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /stocks/momentum-picks:
    get:
      operationId: getMomentumPicks
      tags: [stocks]
      summary: Get price-action S/R stock picks with SL/Target
      description: Returns filtered stocks from the most active sectors, enriched with price-action support/resistance signals, direction, SL, targets, reward:risk, and smart exit rules. Also returns top 5 intraday picks.
      responses:
        "200":
          description: Price-action S/R stock picks grouped by sector
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MomentumPicksResponse"
        "500":
          description: Failed to fetch data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /stocks/trades/history:
    get:
      operationId: getTradeHistory
      tags: [stocks]
      summary: Get historical trade signals from the database
      description: Returns trade signals from the last N days (default 30, max 90) grouped by date, with estimated P&L per trade based on final status.
      parameters:
        - in: query
          name: days
          schema:
            type: integer
            default: 30
            minimum: 1
            maximum: 90
          description: Number of past days to fetch
      responses:
        "200":
          description: Trade history grouped by date
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TradeHistoryResponse"
        "500":
          description: Failed to fetch data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status
    MarketIndex:
      type: object
      properties:
        symbol:
          type: string
        ltp:
          type: number
        changePct:
          type: number
      required:
        - symbol
        - ltp
        - changePct
    SectorPerformance:
      type: object
      properties:
        name:
          type: string
        keyword:
          type: string
        changePct:
          type: number
      required:
        - name
        - keyword
        - changePct
    StockItem:
      type: object
      properties:
        symbol:
          type: string
        ltp:
          type: number
        changePct:
          type: number
        vwap:
          type: ["number", "null"]
        ema20:
          type: ["number", "null"]
        confirmedClose:
          type: ["number", "null"]
        entrySignal:
          type: ["boolean", "null"]
        direction:
          type: ["string", "null"]
          enum: ["LONG", "SHORT", null]
          description: Trade direction from the price-action S/R setup
        setup:
          type: ["string", "null"]
          description: Price-action setup name, such as support rejection or resistance breakout
        sl:
          type: ["number", "null"]
          description: Stop loss price from the active support/resistance zone plus ATR buffer
        target1:
          type: ["number", "null"]
          description: First scale target, one risk unit from entry
        target2:
          type: ["number", "null"]
          description: Final target at the next opposite S/R zone, or fallback 1.5R target
        riskPct:
          type: ["number", "null"]
          description: Risk percentage from entry to SL
        rewardRisk:
          type: ["number", "null"]
          description: Reward-to-risk ratio for the final S/R target
        smartExit:
          type: ["string", "null"]
          description: Plain-English smart exit rule for this trade
        sparkline:
          type: array
          items:
            type: number
          description: Downsampled intraday close prices (up to 40 pts) for sparkline rendering
        circuitLimit:
          type: ["string", "null"]
          enum: ["upper", "lower", null]
          description: Set to 'upper' or 'lower' if last 3 candles have identical close (frozen price = circuit hit), null otherwise
        volumeRatio:
          type: ["number", "null"]
          description: Last confirmed candle volume divided by the recent 20-candle average volume (e.g. 1.8 = 1.8x average)
        volumeOk:
          type: ["boolean", "null"]
          description: True if volumeRatio is at least 1.15 (price-action volume confirmation), false otherwise
        signalTime:
          type: ["string", "null"]
          description: UTC ISO timestamp of when the signal fired
      required:
        - symbol
        - ltp
        - changePct
    TopPick:
      type: object
      description: One of the top 5 intraday picks across all sectors
      properties:
        symbol:
          type: string
        sectorName:
          type: string
        ltp:
          type: number
        changePct:
          type: number
        entry:
          type: number
          description: Confirmed close (entry price)
        sl:
          type: number
          description: Stop loss price
        target1:
          type: number
          description: First scale target, one risk unit from entry
        target2:
          type: number
          description: Final target at the next opposite S/R zone, or fallback 1.5R target
        riskPct:
          type: number
          description: Risk % from entry to SL
        rewardRisk:
          type: number
          description: Reward-to-risk ratio for the final S/R target
        direction:
          type: string
          enum: ["LONG", "SHORT"]
          description: Trade direction from the price-action S/R setup
        setup:
          type: string
          description: Price-action setup name
        smartExit:
          type: string
          description: Smart exit rule
        vwap:
          type: number
        ema20:
          type: number
        sparkline:
          type: array
          items:
            type: number
          description: Downsampled intraday close prices for sparkline rendering
        circuitLimit:
          type: ["string", "null"]
          enum: ["upper", "lower", null]
          description: Set to 'upper' or 'lower' if last 3 candles have identical close (frozen price = circuit hit), null otherwise
        volumeRatio:
          type: ["number", "null"]
          description: Last confirmed candle volume divided by the recent 20-candle average volume
        volumeOk:
          type: ["boolean", "null"]
          description: True if volumeRatio is at least 1.15 (price-action volume confirmation)
        signalTime:
          type: ["string", "null"]
          description: UTC ISO timestamp of when the signal fired
      required:
        - symbol
        - sectorName
        - ltp
        - changePct
        - entry
        - sl
        - target1
        - target2
        - riskPct
        - rewardRisk
        - direction
        - setup
        - smartExit
        - vwap
        - ema20
    SectorWithStocks:
      type: object
      properties:
        sectorName:
          type: string
        sectorKeyword:
          type: string
        sectorChangePct:
          type: number
        stocks:
          type: array
          items:
            $ref: "#/components/schemas/StockItem"
      required:
        - sectorName
        - sectorKeyword
        - sectorChangePct
        - stocks
    MomentumPicksResponse:
      type: object
      properties:
        fetchedAt:
          type: string
          description: ISO timestamp (UTC) of when data was fetched
        indicatorDate:
          type: ["string", "null"]
          description: IST date of the trading session used for indicators (YYYY-MM-DD)
        isLiveSession:
          type: boolean
          description: True if indicator data is from today's live session in IST
        lastCandleTimeIST:
          type: ["string", "null"]
          description: IST time (HH:MM) of the last confirmed candle used for signals
        topPicks:
          type: array
          description: Top 5 intraday picks across all sectors (entry signal stocks sorted by S/R setup score)
          items:
            $ref: "#/components/schemas/TopPick"
        sectors:
          type: array
          items:
            $ref: "#/components/schemas/SectorWithStocks"
      required:
        - fetchedAt
        - isLiveSession
        - topPicks
        - sectors
    HistoryTrade:
      type: object
      properties:
        id:
          type: integer
        symbol:
          type: string
        date:
          type: string
          description: YYYY-MM-DD (IST)
        signalTime:
          type: string
          description: UTC ISO timestamp of when the signal fired
        entryPrice:
          type: string
        sl:
          type: string
        target1:
          type: string
        target2:
          type: string
        direction:
          type: string
          enum: ["LONG", "SHORT"]
          description: Inferred trade direction based on entry, SL, and target
        status:
          type: string
          description: Final trade status (TARGET 2 HIT, TARGET 1 HIT, SL HIT, T1 HIT & TRAILING SL HIT, ENTRY INVALID, legacy VWAP EXIT, SQUARED OFF, ACTIVE, PENDING)
        plPct:
          type: ["number", "null"]
          description: Estimated P&L % based on final status. ENTRY INVALID uses the first post-signal candle close beyond the entry zone. Legacy VWAP EXIT rows use the first post-signal candle close beyond VWAP. SQUARED OFF uses the latest available candle closing at or before 15:15 IST. Null for ACTIVE/PENDING trades or when exit data is unavailable.
      required:
        - id
        - symbol
        - date
        - signalTime
        - entryPrice
        - sl
        - target1
        - target2
        - direction
        - status
    TradeHistoryDaySummary:
      type: object
      properties:
        total:
          type: integer
        winners:
          type: integer
        losers:
          type: integer
        breakeven:
          type: integer
        pending:
          type: integer
      required: [total, winners, losers, breakeven, pending]
    TradeHistoryDay:
      type: object
      properties:
        date:
          type: string
          description: YYYY-MM-DD
        trades:
          type: array
          items:
            $ref: "#/components/schemas/HistoryTrade"
        summary:
          $ref: "#/components/schemas/TradeHistoryDaySummary"
      required: [date, trades, summary]
    TradeHistoryResponse:
      type: object
      properties:
        days:
          type: array
          items:
            $ref: "#/components/schemas/TradeHistoryDay"
      required: [days]
    ErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error
